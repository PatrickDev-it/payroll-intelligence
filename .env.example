# Git commit SHA or immutable release id. CI and Docker set both explicitly;
# Vercel derives both from VERCEL_GIT_COMMIT_SHA in next.config.ts.
PAYROLL_RELEASE=local
NEXT_PUBLIC_PAYROLL_RELEASE=local
